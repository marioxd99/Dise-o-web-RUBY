<div style="margin-left: 20vw;">
<%= form_with(model: collection) do |form| %>
  <% if collection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(collection.errors.count, "error") %> prohibited this collection from being saved:</h2>

      <ul>
        <% collection.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %><br/>
    <%= form.text_field :title %>
  </div>
  <br/>
  
  <div class="field form-group">
    <%= form.text_field :user_id ,id: :note_user_id , type: :hidden, class:"form-control", value:current_user.id %>
  </div>
  <br/>

  <div class="field">
    <%= form.submit %>
  </div>
    <br/>
  <%= link_to 'Back', collections_path, class:"btn btn-secondary" %>
<% end %>
</div>